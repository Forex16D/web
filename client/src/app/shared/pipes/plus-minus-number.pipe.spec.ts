import { PlusMinusNumberPipe } from './plus-minus-number.pipe';

describe('PlusMinusNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PlusMinusNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
